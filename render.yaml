services:
  - type: web
    name: my-api
    env: ruby
    plan: free
    buildCommand: |
      bundle install
      RAILS_ENV=production bin/rails db:migrate
    startCommand: bin/rails server -b 0.0.0.0 -p $PORT